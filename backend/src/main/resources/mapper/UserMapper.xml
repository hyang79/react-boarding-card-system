<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.loginbackend.mapper.UserMapper">

    <!-- Result Map -->
    <resultMap id="UserResultMap" type="com.example.loginbackend.entity.User">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="role" column="role" javaType="com.example.loginbackend.entity.User$Role" jdbcType="VARCHAR"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 이메일로 사용자 조회 -->
    <select id="findByEmail" resultMap="UserResultMap">
        SELECT id, email, password, name, role, created_at, updated_at
        FROM users
        WHERE email = #{email}
    </select>

    <!-- ID로 사용자 조회 -->
    <select id="findById" resultMap="UserResultMap">
        SELECT id, email, password, name, role, created_at, updated_at
        FROM users
        WHERE id = #{id}
    </select>

    <!-- 이메일 존재 여부 확인 -->
    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM users
        WHERE email = #{email}
    </select>

    <!-- 사용자 저장 -->
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (email, password, name, role, created_at, updated_at)
        VALUES (#{email}, #{password}, #{name}, #{role}, NOW(), NOW())
    </insert>

    <!-- 사용자 정보 업데이트 -->
    <update id="update">
        UPDATE users
        SET name = #{name},
            role = #{role},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 사용자 삭제 -->
    <delete id="deleteById">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <!-- 전체 사용자 수 조회 -->
    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM users
    </select>

</mapper>